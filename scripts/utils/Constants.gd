extends Node


enum feature_flag {
	NONE,
	HAS_DAGGER,
	HAS_ICE_SPELL,
	HAS_WIND_SPELL
}

enum checkpoint {
	NONE,
	DAGGER,
	ICE_SPELL,
	WIND_SPELL,
	DAGGER_FLASHBACK,
	ICE_SPELL_FLASHBACK,
	WIND_SPELL_FLASHBACK
}

enum strength_shards {
	
}

enum health_shards {
	
}
